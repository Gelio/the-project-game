export interface BoardSize {
  /**
   * Board width
   */
  x: number;

  /**
   * Task (neutral) area height
   */
  taskArea: number;

  /**
   * Each team's goal area height
   */
  goalArea: number;
}
